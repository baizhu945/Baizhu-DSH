/**
 * dsh-openai-account-ui — host half.
 *
 * Host 侧没有逻辑：登录/登出/状态由 `openai-codex-account` 插件的斜杠命令
 * 提供，本包只负责把浏览器 UI（client.js）挂到 dsh 的设置页。空的 apply
 * 让本行出现在 host cordis 组合中，client-modules 通过 package.json 的
 * dsh.client 声明发现并加载浏览器半体。
 */
export const name = 'dsh-openai-account-ui'

export function apply() {}

diff --git a/packages/client/ui-tool/src/client/tool/ToolCallTree.module.css b/packages/client/ui-tool/src/client/tool/ToolCallTree.module.css
index b33fb477c..9a43933ec 100644
--- a/packages/client/ui-tool/src/client/tool/ToolCallTree.module.css
+++ b/packages/client/ui-tool/src/client/tool/ToolCallTree.module.css
@@ -10,3 +10,9 @@
   padding-left: 8px;
   border-left: 1px solid var(--dsw-alias-border-l2);
 }
+
+.codexSubCalls {
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+}
diff --git a/packages/client/ui-tool/src/client/tool/ToolCallTree.tsx b/packages/client/ui-tool/src/client/tool/ToolCallTree.tsx
index 2a1194807..187c68b1a 100644
--- a/packages/client/ui-tool/src/client/tool/ToolCallTree.tsx
+++ b/packages/client/ui-tool/src/client/tool/ToolCallTree.tsx
@@ -10,6 +10,11 @@ function callName(node: ToolCallBlock): string {
   return 'kind' in node ? node.call?.name ?? '' : node.name
 }
 
+/** Codex's outer exec call is a transport envelope for its nested calls. */
+function isCodexTransport(block: ToolCallBlock): boolean {
+  return callName(block) === 'exec' && block.subCalls.length > 0
+}
+
 /** One atomic call dispatched through the Tool-owned keyed slot. */
 const ToolCall = memo(function ToolCall({
   renderSlot, callId, toolName, block, openFile, selected, cwd, home, inspectCall, t, children,
@@ -52,6 +57,33 @@ const ToolCallBranch = memo(function ToolCallBranch({
   block: ToolCallBlock
   home?: string | undefined
 }) {
+  const childRows = block.subCalls.map(child => (
+    <ToolCallBranch
+      key={child.callId}
+      renderSlot={renderSlot}
+      block={child}
+      selectedCallId={selectedCallId}
+      cwd={cwd}
+      home={home}
+      openFile={openFile}
+      inspectCall={inspectCall}
+      t={t}
+    />
+  ))
+  if (isCodexTransport(block)) {
+    return (
+      <div
+        className={css.codexSubCalls}
+        data-subcalls
+        data-codex-transport
+        data-chat-anchor-key={'call:' + block.callId}
+        data-chat-call-id={block.callId}
+        data-selected={block.callId === selectedCallId || undefined}
+      >
+        {childRows}
+      </div>
+    )
+  }
   return (
     <ToolCall
       renderSlot={renderSlot}
@@ -65,23 +97,7 @@ const ToolCallBranch = memo(function ToolCallBranch({
       inspectCall={inspectCall}
       t={t}
     >
-      {block.subCalls.length > 0 ? (
-        <div className={css.subCalls} data-subcalls>
-          {block.subCalls.map(child => (
-            <ToolCallBranch
-              key={child.callId}
-              renderSlot={renderSlot}
-              block={child}
-              selectedCallId={selectedCallId}
-              cwd={cwd}
-              home={home}
-              openFile={openFile}
-              inspectCall={inspectCall}
-              t={t}
-            />
-          ))}
-        </div>
-      ) : null}
+      {childRows.length > 0 ? <div className={css.subCalls} data-subcalls>{childRows}</div> : null}
     </ToolCall>
   )
 })
diff --git a/packages/client/ui-trajectory/src/client/layout.ts b/packages/client/ui-trajectory/src/client/layout.ts
index a811e915f..16fedb42d 100644
--- a/packages/client/ui-trajectory/src/client/layout.ts
+++ b/packages/client/ui-trajectory/src/client/layout.ts
@@ -60,6 +60,7 @@ interface LaidCell {
   toolName?: string
   callId?: string
   subCalls?: readonly ToolCallBlock[]
+  codexTransport?: boolean
 }
 
 interface LaidGroup {
@@ -109,6 +110,11 @@ function layoutEntryOrder(entry: OrderedLayoutEntry): number {
     : entry.seq
 }
 
+/** Codex's outer exec cell is a transport envelope for its nested calls. */
+function isCodexTransport(name: string, subCalls: readonly ToolCallBlock[] | undefined): boolean {
+  return name === 'exec' && (subCalls?.length ?? 0) > 0
+}
+
 function inputCellDetail(node: InputNode, t: TrajectoryTranslate): Pick<
   TrajectoryCellProps,
   | 'text'
@@ -438,11 +444,12 @@ export function deriveTrajectoryLayout(
       if (!emittedCallIds.has(node.callId)) {
         const toolName = node.call?.name
         const resultPreview = summarizeResult(node, t)
-        const laidList: LaidCell[] = [{
+        const laidList = withSubCalls([{
           absTime: finiteTime(node.callTime ?? node.time),
           ...(toolName !== undefined ? { toolName } : {}),
           callId: node.callId,
           subCalls: node.subCalls,
+          codexTransport: toolName !== undefined && isCodexTransport(toolName, node.subCalls),
           cell: {
             index: ++index,
             kind: 'tool',
@@ -459,11 +466,8 @@ export function deriveTrajectoryLayout(
             timeSeconds: durationSeconds(node.time, node.callTime),
             startedAt: finiteTime(node.callTime),
           },
-        }]
-        for (const laid of expandSubCalls(node.subCalls, index, t)) {
-          laidList.push(laid)
-          index = laid.cell.index
-        }
+        }], t)
+        if (laidList.length > 0) index = laidList[laidList.length - 1]!.cell.index
         pushStep(0, 1, laidList)
       }
       prevAbsTime = finiteTime(node.time) ?? prevAbsTime
@@ -494,11 +498,12 @@ export function deriveTrajectoryLayout(
   const seenCalls = collectCallIds(turns)
   for (const call of runningCalls) {
     if (seenCalls.has(call.callId)) continue
-    const laidList: LaidCell[] = [{
+    const laidList = withSubCalls([{
       absTime: null,
       toolName: call.name,
       callId: call.callId,
       subCalls: call.subCalls,
+      codexTransport: isCodexTransport(call.name, call.subCalls),
       cell: {
         index: ++index,
         kind: 'tool',
@@ -508,11 +513,8 @@ export function deriveTrajectoryLayout(
         timeSeconds: null,
         startedAt: finiteTime(call.time),
       },
-    }]
-    for (const laid of expandSubCalls(call.subCalls, index, t)) {
-      laidList.push(laid)
-      index = laid.cell.index
-    }
+    }], t)
+    if (laidList.length > 0) index = laidList[laidList.length - 1]!.cell.index
     if (call.step > 0) pushStep(call.turn, call.step, laidList)
     else for (const laid of laidList) pushMessage(call.turn, laid)
   }
@@ -762,6 +764,7 @@ function expandAssistant(
       toolName: block.name,
       callId: block.callId,
       ...(call === undefined ? {} : { subCalls: call.subCalls }),
+      codexTransport: isCodexTransport(block.name, call?.subCalls),
       cell: {
         index: ++index, kind: 'tool',
         ...summarizeCall(block.name, block.argsRaw),
@@ -981,6 +984,13 @@ function withSubCalls(laidList: LaidCell[], t: TrajectoryTranslate): LaidCell[]
   const out: LaidCell[] = []
   let index = laidList[0] !== undefined ? laidList[0].cell.index - 1 : 0
   for (const laid of laidList) {
+    if (laid.codexTransport === true && laid.subCalls !== undefined && laid.subCalls.length > 0) {
+      for (const sub of expandSubCalls(laid.subCalls, index, t, 'tool')) {
+        out.push(sub)
+        index = sub.cell.index
+      }
+      continue
+    }
     out.push({ ...laid, cell: { ...laid.cell, index: ++index } })
     for (const sub of expandSubCalls(laid.subCalls, index, t)) {
       out.push(sub)
@@ -995,6 +1005,7 @@ function expandSubCalls(
   subs: readonly ToolCallBlock[] | undefined,
   startIndex: number,
   t: TrajectoryTranslate,
+  rowKind: 'subtool' | 'tool' = 'subtool',
 ): LaidCell[] {
   if (subs === undefined || subs.length === 0) return []
   const out: LaidCell[] = []
@@ -1008,7 +1019,7 @@ function expandSubCalls(
       callId: sub.callId,
       cell: {
         index: ++index,
-        kind: 'subtool',
+        kind: rowKind,
         callId: sub.callId,
         ...(settled
           ? (sub.call !== null

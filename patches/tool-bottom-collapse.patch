diff --git a/packages/client/ui-chat/src/client/chat/ReasoningRow.module.css b/packages/client/ui-chat/src/client/chat/ReasoningRow.module.css
index 2d7cbe8f0..8342000ac 100644
--- a/packages/client/ui-chat/src/client/chat/ReasoningRow.module.css
+++ b/packages/client/ui-chat/src/client/chat/ReasoningRow.module.css
@@ -78,6 +78,27 @@
   word-break: break-word;
 }
 
+.collapseButton {
+  display: inline-flex;
+  align-self: flex-start;
+  align-items: center;
+  gap: 4px;
+  margin: 4px 0 2px 22px;
+  padding: 2px 8px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 999px;
+  background: var(--dsw-alias-bg-base);
+  color: var(--dsw-alias-label-secondary);
+  font-size: 11px;
+  line-height: 16px;
+  cursor: pointer;
+}
+
+.collapseButton:hover {
+  background: var(--dsw-alias-interactive-bg-hover-solid);
+  color: var(--dsw-alias-label-primary);
+}
+
 @media (prefers-reduced-motion: reduce) {
   .root[data-state='running'] .row::after {
     animation: none;
diff --git a/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx b/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx
index 1f5fba56b..b155b8d7e 100644
--- a/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx
+++ b/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx
@@ -1,6 +1,6 @@
 /** Assistant reasoning disclosure, independent of Tool-call presentation. */
 import { useEffect, useRef, useState } from 'react'
-import { DisclosureRow, IconThinkOutline14 } from '@deepseek-ai/dsh-client-ui-primitives'
+import { DisclosureRow, IconChevronUpOutline14, IconThinkOutline14 } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { ChatViewSlotProps } from '../contract/slots.ts'
 import { useThrottledVisualUpdate } from './use-throttled-visual-update.ts'
 import a11yCss from './accessibility.module.css'
@@ -59,6 +59,14 @@ export function ReasoningRow({ text, running, t }: { text: string; running: bool
         )}
       >
         <div className={css.thinkBody}>{text}</div>
+        <button
+          type="button"
+          className={css.collapseButton}
+          onClick={() => { setExpanded(false) }}
+        >
+          <IconChevronUpOutline14 />
+          {t('row.collapse')}
+        </button>
       </DisclosureRow>
     </div>
   )
diff --git a/packages/client/ui-chat/src/client/locale.ts b/packages/client/ui-chat/src/client/locale.ts
index f10a9ced2..ab701c99d 100644
--- a/packages/client/ui-chat/src/client/locale.ts
+++ b/packages/client/ui-chat/src/client/locale.ts
@@ -112,6 +112,7 @@ export const zh = {
   'command.title': '指令',
   'row.running': '运行中',
   'row.failed': '失败',
+  'row.collapse': '折叠',
   'json.truncated': '… 已截断，共 {total} 字符',
   'clock.md': '{m}月{d}日',
   'clock.ymd': '{y}年{m}月{d}日',
@@ -229,6 +230,7 @@ export const en = {
   'command.title': 'Command',
   'row.running': 'Running',
   'row.failed': 'Failed',
+  'row.collapse': 'Collapse',
   'json.truncated': '… truncated, {total} characters total',
   'clock.md': '{m}/{d}',
   'clock.ymd': '{y}-{m}-{d}',
diff --git a/packages/client/ui-conversation/src/client/locales.ts b/packages/client/ui-conversation/src/client/locales.ts
index 296f95b33..2db5315c9 100644
--- a/packages/client/ui-conversation/src/client/locales.ts
+++ b/packages/client/ui-conversation/src/client/locales.ts
@@ -91,6 +91,7 @@ export const zh = {
   'row.input': '输入',
   'row.output': '输出',
   'row.inspect': '查看',
+  'row.collapse': '折叠',
   'tool.title.search': '搜索',
   'tool.title.read': '读取',
   'tool.title.bash': 'Bash',
@@ -241,6 +242,7 @@ export const en = {
   'row.input': 'IN',
   'row.output': 'OUT',
   'row.inspect': 'Inspect',
+  'row.collapse': 'Collapse',
   'tool.title.search': 'Search',
   'tool.title.read': 'Read',
   'tool.title.bash': 'Bash',
diff --git a/packages/client/ui-skill/src/client/SkillRow.module.css b/packages/client/ui-skill/src/client/SkillRow.module.css
index 05c3388aa..bb21f2b59 100644
--- a/packages/client/ui-skill/src/client/SkillRow.module.css
+++ b/packages/client/ui-skill/src/client/SkillRow.module.css
@@ -189,6 +189,27 @@
   color: var(--dsw-alias-label-primary);
 }
 
+.collapseButton {
+  display: inline-flex;
+  align-self: flex-start;
+  align-items: center;
+  gap: 4px;
+  margin: 4px 0 2px 4px;
+  padding: 2px 8px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 999px;
+  background: var(--dsw-alias-bg-base);
+  color: var(--dsw-alias-label-secondary);
+  font-size: 11px;
+  line-height: 16px;
+  cursor: pointer;
+}
+
+.collapseButton:hover {
+  background: var(--dsw-alias-interactive-bg-hover-solid);
+  color: var(--dsw-alias-label-primary);
+}
+
 .visuallyHidden {
   position: absolute;
   width: 1px;
diff --git a/packages/client/ui-skill/src/client/SkillRow.tsx b/packages/client/ui-skill/src/client/SkillRow.tsx
index bee776d93..6bb2c96fe 100644
--- a/packages/client/ui-skill/src/client/SkillRow.tsx
+++ b/packages/client/ui-skill/src/client/SkillRow.tsx
@@ -1,6 +1,6 @@
 import { useState, type KeyboardEvent, type ReactNode } from 'react'
 import {
-  IconChevronDownOutline14, IconInspectOutline12, IconSkillOutline16, StateDot,
+  IconChevronDownOutline14, IconChevronUpOutline14, IconInspectOutline12, IconSkillOutline16, StateDot,
 } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { ToolCallViewProps } from '@deepseek-ai/dsh-client-ui-tool/client'
 import type { PropsLocale } from '@deepseek-ai/dsh-client-ui-slots'
@@ -159,6 +159,10 @@ export function SkillRow({ block, inspect, t }: SkillRowProps) {
               {t('row.inspect')}
             </button>
           ) : null}
+          <button type="button" className={css.collapseButton} onClick={() => { setExpanded(false) }}>
+            <IconChevronUpOutline14 />
+            {t('row.collapse')}
+          </button>
         </div>
       ) : null}
     </div>
diff --git a/packages/client/ui-skill/src/client/locales.ts b/packages/client/ui-skill/src/client/locales.ts
index d7a9b9422..42368a009 100644
--- a/packages/client/ui-skill/src/client/locales.ts
+++ b/packages/client/ui-skill/src/client/locales.ts
@@ -11,6 +11,7 @@ export const zh = {
   'row.stopped': 'skill 加载已中止',
   'row.instructions': '说明',
   'row.inspect': '查看',
+  'row.collapse': '折叠',
   'menu.userOnly': '仅用户',
 } satisfies Record<string, string>
 
@@ -25,5 +26,6 @@ export const en = {
   'row.stopped': 'Skill load stopped',
   'row.instructions': 'Instructions',
   'row.inspect': 'Inspect',
+  'row.collapse': 'Collapse',
   'menu.userOnly': 'user-only',
 } satisfies Record<SkillKey, string>
diff --git a/packages/client/ui-tool/src/client/tool/components/ToolRow.module.css b/packages/client/ui-tool/src/client/tool/components/ToolRow.module.css
index 2f344ac0d..c19270c19 100644
--- a/packages/client/ui-tool/src/client/tool/components/ToolRow.module.css
+++ b/packages/client/ui-tool/src/client/tool/components/ToolRow.module.css
@@ -196,6 +196,27 @@
   color: var(--dsw-alias-label-primary);
 }
 
+.collapseButton {
+  display: inline-flex;
+  align-self: flex-start;
+  align-items: center;
+  gap: 4px;
+  margin: 4px 0 2px 4px;
+  padding: 2px 8px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 999px;
+  background: var(--dsw-alias-bg-base);
+  color: var(--dsw-alias-label-secondary);
+  font-size: 11px;
+  line-height: 16px;
+  cursor: pointer;
+}
+
+.collapseButton:hover {
+  background: var(--dsw-alias-interactive-bg-hover-solid);
+  color: var(--dsw-alias-label-primary);
+}
+
 /* Expanded-body scroll wrapper for the run_code CodeBlock; the IN/OUT card
    and the terminal card scroll INSIDE their own surface instead, so the
    scrollbar sits within the rounded card. */
diff --git a/packages/client/ui-tool/src/client/tool/components/ToolRow.tsx b/packages/client/ui-tool/src/client/tool/components/ToolRow.tsx
index 06029777a..9b7f40136 100644
--- a/packages/client/ui-tool/src/client/tool/components/ToolRow.tsx
+++ b/packages/client/ui-tool/src/client/tool/components/ToolRow.tsx
@@ -1,7 +1,7 @@
 import { useMemo, useState, type KeyboardEvent, type MouseEvent, type ReactNode } from 'react'
 import clsx from 'clsx'
 import {
-  CodeBlock, DiffBlock, DisclosureRow, IconInspectOutline12, ReadBlock, SearchBlock, StateDot, TerminalBlock, WebBlock,
+  CodeBlock, DiffBlock, DisclosureRow, IconChevronUpOutline14, IconInspectOutline12, ReadBlock, SearchBlock, StateDot, TerminalBlock, WebBlock,
   diffTotals,
 } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { TranslateNS } from '@deepseek-ai/dsh-client-ui-slots'
@@ -279,6 +279,14 @@ export function ToolRow({
               {t('row.inspect')}
             </button>
           )}
+          <button
+            type="button"
+            className={css.collapseButton}
+            onClick={() => { setExpanded(false) }}
+          >
+            <IconChevronUpOutline14 />
+            {t('row.collapse')}
+          </button>
         </div>
       </DisclosureRow>
     </div>
diff --git a/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.module.css b/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.module.css
index 4c1d7b18b..bd628069e 100644
--- a/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.module.css
+++ b/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.module.css
@@ -240,6 +240,27 @@
   color: var(--dsw-alias-label-primary);
 }
 
+.collapseButton {
+  display: inline-flex;
+  align-self: flex-start;
+  align-items: center;
+  gap: 4px;
+  margin: 4px 0 2px 4px;
+  padding: 2px 8px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 999px;
+  background: var(--dsw-alias-bg-base);
+  color: var(--dsw-alias-label-secondary);
+  font-size: 11px;
+  line-height: 16px;
+  cursor: pointer;
+}
+
+.collapseButton:hover {
+  background: var(--dsw-alias-interactive-bg-hover-solid);
+  color: var(--dsw-alias-label-primary);
+}
+
 
 .visuallyHidden {
   position: absolute;
diff --git a/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.tsx b/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.tsx
index c291a733a..36fd5796a 100644
--- a/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.tsx
+++ b/packages/client/ui-tool/src/client/tool/toolviews/bash-sample.tsx
@@ -2,7 +2,7 @@ import { useMemo, useState, type KeyboardEvent } from 'react'
 import type { Context } from '@deepseek-ai/cordis'
 import clsx from 'clsx'
 import {
-  IconApiOutline14, IconChevronDownOutline14, IconInspectOutline12, StateDot, TerminalBlock,
+  IconApiOutline14, IconChevronDownOutline14, IconChevronUpOutline14, IconInspectOutline12, StateDot, TerminalBlock,
 } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { PropsLocale } from '@deepseek-ai/dsh-client-ui-slots'
 import type { ToolCallViewProps } from '../../contract/slots.ts'
@@ -146,6 +146,10 @@ export function BashRow({ toolName, block, sessionId, useSessions, inspect, t }:
               {t('row.inspect')}
             </button>
           )}
+          <button type="button" className={css.collapseButton} onClick={() => { setExpanded(false) }}>
+            <IconChevronUpOutline14 />
+            {t('row.collapse')}
+          </button>
         </div>
       )}
     </div>
diff --git a/packages/extensions/ui-cordis/src/client/CordisDefineRow.module.css b/packages/extensions/ui-cordis/src/client/CordisDefineRow.module.css
index fdc2ebbf5..5493eca81 100644
--- a/packages/extensions/ui-cordis/src/client/CordisDefineRow.module.css
+++ b/packages/extensions/ui-cordis/src/client/CordisDefineRow.module.css
@@ -281,6 +281,27 @@
   color: var(--dsw-alias-label-primary);
 }
 
+.collapseButton {
+  display: inline-flex;
+  align-self: flex-start;
+  align-items: center;
+  gap: 4px;
+  margin: 4px 0 2px 4px;
+  padding: 2px 8px;
+  border: 1px solid var(--dsw-alias-border-l2);
+  border-radius: 999px;
+  background: var(--dsw-alias-bg-base);
+  color: var(--dsw-alias-label-secondary);
+  font-size: 11px;
+  line-height: 16px;
+  cursor: pointer;
+}
+
+.collapseButton:hover {
+  background: var(--dsw-alias-interactive-bg-hover-solid);
+  color: var(--dsw-alias-label-primary);
+}
+
 .visuallyHidden {
   position: absolute;
   width: 1px;
diff --git a/packages/extensions/ui-cordis/src/client/CordisDefineRow.tsx b/packages/extensions/ui-cordis/src/client/CordisDefineRow.tsx
index 0f2207c59..27afaee53 100644
--- a/packages/extensions/ui-cordis/src/client/CordisDefineRow.tsx
+++ b/packages/extensions/ui-cordis/src/client/CordisDefineRow.tsx
@@ -2,7 +2,7 @@
 
 import { useId, useState, type ReactNode } from 'react'
 import {
-  CodeBlock, DisclosureRow, IconCodeOutline16, IconInspectOutline12, StateDot,
+  CodeBlock, DisclosureRow, IconChevronUpOutline14, IconCodeOutline16, IconInspectOutline12, StateDot,
 } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { InjectFace, PropsLocale } from '@deepseek-ai/dsh-client-ui-slots'
 import type { ToolCallViewProps } from '@deepseek-ai/dsh-client-ui-tool/client'
@@ -164,6 +164,10 @@ export function CordisDefineRow({
               {t('action.inspect')}
             </button>
           )}
+          <button type="button" className={css.collapseButton} onClick={() => { setExpanded(false) }}>
+            <IconChevronUpOutline14 />
+            {t('action.collapse')}
+          </button>
         </div>
       </DisclosureRow>
     </div>
diff --git a/packages/extensions/ui-cordis/src/client/locales.ts b/packages/extensions/ui-cordis/src/client/locales.ts
index 2c410a8f0..06a14ee37 100644
--- a/packages/extensions/ui-cordis/src/client/locales.ts
+++ b/packages/extensions/ui-cordis/src/client/locales.ts
@@ -43,6 +43,7 @@ export const zh = {
   'action.retry': '重试',
   'action.rollback': '回退',
   'action.inspect': '查看',
+  'action.collapse': '折叠',
   'render.failedAbdicated': '{slot} 渲染失败，已恢复默认界面：',
   'render.failedHeld': '{slot} 渲染失败：',
   'a11y.defining': '正在定义插件',
@@ -107,6 +108,7 @@ export const en = {
   'action.retry': 'Retry',
   'action.rollback': 'Roll back',
   'action.inspect': 'Inspect',
+  'action.collapse': 'Collapse',
   'render.failedAbdicated': 'Rendering failed in {slot}; the default UI was restored:',
   'render.failedHeld': 'Rendering failed in {slot}:',
   'a11y.defining': 'Defining the plugin',
